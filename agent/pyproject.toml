[project]
name = "inact-agent"
version = "0.1.0"
description = "Inact reply agent — auto-replies to messages using OpenRouter via PydanticAI"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai>=0.1",
    "flask>=3.0",
    "requests>=2.31",
    "gunicorn>=21.0",
]
