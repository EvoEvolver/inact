services:
  agent:
    build:
      context: ..          # repo root so Dockerfile can COPY agent/
      dockerfile: agent/Dockerfile
    ports:
      - "${PORT:-7779}:7779"
    env_file:
      - .env
    restart: unless-stopped
